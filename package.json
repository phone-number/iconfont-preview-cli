{
  "name": "iconfont-preview-cli",
  "version": "1.0.1",
  "author": "wangzl",
  "description": "字体图标cli",
  "scripts": {
    "dev:app": "pnpm run -C app dev",
    "dev:server": "pnpm run -C server dev",
    "dev": "pnpm run dev:server & pnpm run dev:app",
    "preview": "node dist/server/bin.mjs --dir font-icon",
    "build:app": "pnpm run -C app build",
    "build:other": "pnpm run -C build-other build",
    "build:server": "pnpm run -C server build",
    "build": "pnpm run -r --parallel build",
    "del:node_modules": "rimraf \"**/node_modules\"",
    "del:dist": "rimraf dist"
  },
  "keywords": [
    "iconfont",
    "字体图标",
    "字体图标预览",
    "字体图标vite插件"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/phone-number/iconfont-preview-cli.git"
  },
  "bin": {
    "iconfont-preview-cli": "./dist/server/bin.mjs"
  },
  "exports": {
    ".": {
      "import": "./dist/server/index.mjs",
      "require": "./dist/server/index.cjs"
    },
    "./components": {
      "import": "./dist/components/index.mjs",
      "require": "./dist/components/index.cjs"
    },
    "./server": {
      "import": "./dist/server/index.mjs",
      "require": "./dist/server/index.cjs"
    },
    "./*": "./dist/*"
  },
  "typesVersions": {
    "*": {
      "components": [
        "./dist/components/types/index.d.ts"
      ],
      "server": [
        "./dist/server/index.d.ts"
      ]
    }
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  },
  "pnpm": {
    "overrides": {
      "escape-string-regexp": "4.0.0"
    }
  },
  "engines": {
    "node": ">=18"
  }
}
